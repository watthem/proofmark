#!/bin/sh
# Block pushes that would ship stale docs to proofmark.dev/docs.
# Enable once with: git config core.hooksPath scripts/hooks
npm run docs:check || {
  echo "Push aborted: docs are out of date. Commit the rebuilt site/docs and retry."
  exit 1
}

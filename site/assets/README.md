# Site Assets

- `proofmark-dashboard-demo.png` is the public product screenshot used by the
  static landing page.
- `screenshots/` contains durable review captures for desktop and mobile checks.

Screenshot files in this folder replace the temporary `/tmp/proofmark-*.png`
captures used during local review.
